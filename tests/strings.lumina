import "../std/io.lumina"
import "../std/string.lumina"
import "../std/collections.lumina"

var listString = toString("a, b, c, d");
var list = split(listString, ", ");

var i = 0;
while (i < list.size) {
	var item = list.get(i);
	print(item.list);
	print(list.get(i).list);
	i = i + 1;
}

print("\n");

var string = join(list, "/");

print(string.list);
print("\n");

var newstring = createArray<char>();

newstring.extend(toString("Hello"));
print(newstring.list);
print("\n");

newstring.push(',');
newstring.push(' ');
print(newstring.list);
print("\n");

newstring.extend(toString("World!"));
print(newstring.list);
print("\n");
