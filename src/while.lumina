import "parser.lumina"
import "lexer.lumina"
import "position.lumina"
import "condition.lumina"
import "name.lumina"
import "block.lumina"


type While {
	Region region;
	
	Name keyword;
	Condition condition;
	Block block;

	int id;

	func parse(While self, Parser parser, Lexer lexer) -> While {
		self.region = lexer.startRegion();

		self.keyword = lexer.consumeKeyword(KEYWORD_while);

		self.condition = new Condition();
		self.condition.parse(parser, lexer);

		self.block = new Block();
		self.block.parse(parser, lexer);

		lexer.endRegion(self.region);
		return self;
	}
}
