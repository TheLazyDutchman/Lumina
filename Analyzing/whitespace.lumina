import "../std/string.lumina"


type WhiteSpace {
	str value;
	int length;
}

func createSpace(str start) -> WhiteSpace {
	var space = new WhiteSpace();

	space.value = start;
	space.length = strlen(start);

	return space;
}
